name = "word-dict"
main = "worker.js"
compatibility_date = "2024-09-01"

# 部署后请设置 secret:
#   wrangler secret put AGENS_API_KEY
# 或在 Cloudflare Dashboard -> Workers -> word-dict -> Settings -> Variables 中添加 AGENS_API_KEY (Secret 类型)

# 自定义路由（可选）：把 worddict.chatlz.dpdns.org 解析到这个 Worker
# routes = [
#   { pattern = "worddict.chatlz.dpdns.org/*", custom_domain = true }
# ]
